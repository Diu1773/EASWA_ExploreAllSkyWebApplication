# EASWA 배포 서버 동시접속 부하 실측 (2026-07-14 최초 · 2026-07-16 재측정)

교사연수(10~15명 동시)에서 배포 서버가 측광→모델적합 흐름을 견디는지 실측한 결과.

> ## ✅ 최신 결론 (2026-07-16 재측정 — **8절**)
> 방어책이 적용된 현재 프로덕션에서 **동시 10명 · 15명 · 동기화 버스트 15명 모두 전원 성공, 크래시 0, health 200 유지.**
> **10명 29.9초 / 15명 48.4초 / 버스트 15명 45.2초**에 전원 완료 → **10~15명은 이제 추정이 아니라 실측으로 안전.**
> **진짜 리스크는 부하가 아니라 콜드스타트**: 콜드 fit **24.1초** vs 웜 **1.7~2.9초**(약 10배) → **수업 직전 워밍업이 부하 대책보다 중요.**

> ⚠️ **아래 2·4절의 "10명 시 fit 약 12분 / 15명 서버 사망(0/15)"은 방어책 적용 *전*(2026-07-14 최초 측정) 숫자입니다. 현재 코드·배포에는 해당하지 않습니다.** 7절(방어책 반영)·8절(재측정 ②) 참조.

**최초(방어 전) 결론 요약: 10명까지는 전원 성공하지만 fit이 사실상 직렬 처리되어 대기시간이 인원수에 비례해 늘어나고(10명 시 fit 약 12분), 15명 동시에서는 인스턴스가 죽어 fit 성공 0/15. 서버는 약 1분 내 자동 재시작으로 회복.**

> **2026-07-14 방어책 반영 완료 — 7절 참조.** 전역 동시 fit 세마포어(3) + MCMC 기본 OFF 배포 후 재측정: 프로덕션 단독 fit 70초 → warm 약 6초, 동시 6명 전원 성공(11.9초), 붕괴 경로 차단.

## 1. 측정 조건

| 항목 | 값 |
|---|---|
| 대상 서버 | https://easwa-webapp.onrender.com (Render 무료 인스턴스, 단일 uvicorn 워커) |
| 측정 일시 | 2026-07-14 22:25–23:00 KST (측정 시작 시 서버는 이미 warm 상태 — 콜드스타트 미포함) |
| 데이터 | TESS FFI cutout, WASP-6 b, Sector 2 (2018-08 관측, 30분 케이던스, 1,245프레임, 50 px 컷아웃) — 배포 이미지에 번들된 실습용 데이터 (`observation_id=wasp_6_b_sector_0002`) |
| 측광 요청 | `POST /api/transit/photometry-stream` (NDJSON 스트림). 앱 기본값 그대로: 구경 반지름 2.5 px / 내부 고리 4.0 / 외부 고리 6.0 (TransitLab.tsx `DEFAULT_APERTURE`), 추천 비교성 자동 선택 8개, preview dataset token 포함 |
| fit 요청 | `POST /api/transit/fit-stream`. 앱 기본 흐름 그대로: `bjd_window` 모드, `computeDefaultBjdWindow` 자동 창(±0.403일 → ROI 37점), baseline 0차, 시그마클립 없음, limb darkening 고정, TESS 필터, 항성 파라미터 포함. 백엔드는 batman + **emcee MCMC(워커 32 × 스텝 1,500)** 실행 |
| 소요시간 정의 | 요청 시작 → 스트림 소비 완료(결과 수신)까지. SSE/NDJSON 스트림을 끝까지 소비 |
| 시나리오 | 단독 1명 → 동시 5명 → 10명 → 15명. 가상 사용자는 3~10초 랜덤 시차로 시작, 각자 측광→fit 순차 수행. 시나리오 사이 60초 휴지 + `/api/health` 확인 |
| 부하 발생 | 로컬 노트북 1대(Python asyncio+httpx, 단일 IP)에서 발생. 병목이 서버 CPU/메모리라 대표성에 문제 없음. 연수장 NAT(전원 동일 공인 IP) 상황과 동일 조건 |

주의: 지시문에 "구경 2.5/4/8"로 기재되어 있었으나 실제 앱 기본값은 **2.5/4.0/6.0**이라 앱 값을 그대로 사용함.

## 2. 시나리오별 결과 — 프로덕션 (Render)

측광/fit 소요시간은 성공 요청 기준, 단위 초.

| 시나리오 | 측광 성공 | 측광 중앙값 | 측광 최대 | fit 성공 | fit 중앙값 | fit 최대 | 1인 전체 소요(중앙값) | 오류 | 종료 후 health |
|---|---|---|---|---|---|---|---|---|---|
| 단독 1명 | 1/1 | 2.2 | 2.2 | 1/1 | 70.0 | 70.0 | 1분 12초 | 없음 | 200 OK |
| 동시 5명 | 5/5 | 11.8 | 29.1 | 5/5 | 340.8 | 345.8 | 5분 53초 | 없음 | 200 OK |
| 동시 10명 | 10/10 | 43.7 | 53.0 | 10/10 | 701.6 | 713.4 | 12분 21초 | 없음 | 200 OK |
| 동시 15명 | 14/15 | 74.0 | 116.1 | **0/15** | — | — | — | 측광 503×1 / fit 503×2, 스트림 절단×12 | **502** |

429(레이트리밋)는 프로덕션에서 한 번도 발생하지 않음(아래 5절).

### 15명 붕괴 타임라인

- 시작 +85초: 첫 503 (user12 측광, Render 프록시가 0.4초 만에 거절 — 인스턴스 이미 불안정)
- +177초 / +197초: fit 요청 2건이 즉시 503
- **+206초: 진행 중이던 fit 스트림 12개가 같은 순간(22:53:05–06) 전부 절단** → 단일 프로세스 사망(512 MB 무료 인스턴스에서 동시 MCMC 12+건에 의한 메모리/CPU 고갈로 추정)
- 직후 `/api/health` 502
- **+약 1분: health 200 복귀(자동 재시작).** 22:59 단독 재실행 시 측광 2.2초 / fit 73.8초 — 기준선과 동일하게 완전 회복. 수동 조치 불필요

## 3. 로컬 대조 — 동시 10명 1회 (localhost:5895, LG gram 16GB)

| 시나리오 | 측광 성공 | 측광 중앙값 | 측광 최대 | fit 성공 | fit 중앙값 | fit 최대 | 1인 전체 소요(중앙값) |
|---|---|---|---|---|---|---|---|
| 동시 10명 (로컬) | 10/10 | 8.8 | 13.2 | 10/10 | 88.0 | 107.3 | 1분 55초 |

- 같은 10명 동시 조건에서 **로컬이 fit 기준 약 8배 빠름** (중앙값 88초 vs 702초). 전원 완료까지 로컬 3분 17초 vs 프로덕션 13분 18초.
- 로컬에서는 측광 레이트리밋(6회/분/IP)이 정상 발동해 429가 4회 발생 → 자동 재시도로 전원 성공 (재시도 대기 ~30–60초는 위 측광 소요시간에 미포함, 전체 소요에는 포함).

## 4. 병목 진단: 측광 < fit

- **측광은 병목이 아님.** 번들 컷아웃 + 메모리 캐시 덕에 단독 2.2초, 15명 동시에도 중앙값 74초로 성공(마지막 1명 503은 인스턴스 붕괴의 전조).
- **fit(emcee MCMC)이 지배적 병목.** 소요시간이 인원수에 거의 정비례: 1명 70초 → 5명 341초(×4.9) → 10명 702초(×10.0). 단일 워커 + Python GIL + 무료 인스턴스의 제한된 CPU에서 CPU-bound MCMC가 완전 직렬화됨. 즉 **서버 전체 처리량은 fit 약 1건/70초로 고정**이며, 시차를 두더라도 총 소요시간은 줄지 않고 개별 대기만 분산됨.
- **붕괴 지점은 12~15건 동시 fit.** 10건까지는 (느리지만) 살아남고, 15명 시나리오에서 동시 fit이 12건에 도달하자 인스턴스 사망. 512 MB 메모리 한도가 유력 원인.

## 5. 부수 발견 (개선 후보)

1. **프로덕션에서 측광 레이트리밋(6회/분/IP)이 작동하지 않음.** 로컬에서는 발동(429×4)했지만 Render에서는 15명 동시에도 0회. uvicorn이 `--proxy-headers` 없이 실행되어 `request.client.host`가 실제 사용자 IP가 아닌 Render 내부 프록시 IP로 잡히고, 프록시 IP가 연결마다 분산되어 버킷이 파편화된 것으로 추정. → 과부하 보호장치가 프로덕션에서 무력화된 상태. `X-Forwarded-For` 신뢰 설정 또는 전역(IP 무관) 동시 fit 상한 도입 검토.
2. **fit에는 레이트리밋 스코프 자체가 없음** (`transit_fit` 스코프가 `rate_limit_service._rate_limit_configs`에 미등록). 서버를 죽인 것이 바로 무제한 동시 fit이므로, 전역 동시 fit 세마포어(예: 2~3) + 초과 시 "대기 중" 응답이 가장 효과적인 방어.
3. 15명 규모에서도 **자동 재시작 회복은 약 1분**으로 빨랐고 데이터 손상 없음. 다만 붕괴 순간 진행 중이던 모든 사용자의 fit이 무통보 실패(스트림 절단)하므로, 수업 중 발생 시 혼란이 큼.

## 6. 연수 운영 권고

1. **동시 fit은 최대 5명, 권장 3명 이하로 통제할 것.** 5명 동시도 fit 대기 약 5.7분(전체 약 6분). 15명 일제 실행은 서버 다운을 유발하므로 금지. 12건 이상 동시 fit이 실측 붕괴 지점.
2. **시간차 실행 필수 — 3명 × 5개 조로 나눠 조별 순차(약 4분 간격)로 fit 실행.** 서버 처리량이 fit 1건/약 70초로 고정이므로 15명 전원 완료에는 어떤 방식이든 최소 약 18분 소요. 측광은 전원 동시에 시켜도 무방(15명 중앙값 74초).
3. **강사 노트북 로컬 서버를 백업으로 반드시 준비.** 동일 10명 부하를 로컬은 3분 17초에 전원 처리(프로덕션의 약 8배). 연수장 Wi-Fi에서 강사 PC IP로 접속시키는 플랜 B를 사전 리허설하고, Render가 죽어도 약 1분 내 자동 회복된다는 점을 진행 시나리오에 반영할 것.

## 7. 방어책 반영 (2026-07-14, 커밋 9743547)

5절의 개선 후보를 같은 날 반영·배포하고 동일 흐름(WASP-6 b Sector 2 번들, 앱 기본 파라미터)으로 재측정한 결과.

### 적용 내용·설정값

| 항목 | 값 (환경변수, 기본값) |
|---|---|
| fit 스트림 전역 동시실행 상한 | `FIT_MAX_CONCURRENCY` = **3** (IP 무관, `/api/transit/fit-stream`) |
| 측광 스트림 전역 동시실행 상한 | `PHOTOMETRY_MAX_CONCURRENCY` = **8** (기존 per-IP 6회/분 리밋은 그대로 유지) |
| 대기 한도 | `STREAM_QUEUE_WAIT_SECONDS` = **90초** — 대기 중 3초 간격으로 NDJSON `queued` 진행 이벤트(대기 순번 포함, 한국어), 초과 시 한국어 오류 이벤트로 종료 |
| MCMC 정밀화 | `FIT_MCMC_DEFAULT` = **false** (기본은 최소제곱 + 야코비안 불확도, `used_mcmc=false`로 방법 노출). 요청 `refine_mcmc=true`로 켤 수 있고, 켤 때 `FIT_MCMC_WALKERS`/`FIT_MCMC_STEPS` = **16×500** (기존 32×1,500) |
| LSQ 강건화 | a/R* 다중 시작점(초기 추정 + 4/7/12/20) 최소제곱 후 최저 비용 해 선택 — 단일 시작이 스침(grazing) 국소해(Rp/R* 0.398, χ²ᵣ 10.0)로 수렴하던 문제 수정 |

환경변수는 `EASWA_` 접두 이름도 동일하게 인식. 게이트는 FIFO이며 클라이언트 이탈/타임아웃 시 슬롯을 회수한다(슬롯 누수 없음 실측 확인).

### 결과값 정확성 (WASP-6 b Sector 2, 앱 기본 fit 조건)

| 경로 | Rp/R* | depth | a/R* | χ²ᵣ |
|---|---|---|---|---|
| 기본(LSQ, 프로덕션) | 0.14534 ± 0.00591 | 2.11 % | 11.26 | 0.957 |
| refine_mcmc(16×500, 프로덕션) | 0.14393 ± 0.00403 | 2.07 % | 11.51 | 0.955 |

두 경로가 불확도 내 일치(|ΔRp/R*| = 0.0014 < 오차합 0.0099), NASA Exoplanet Archive 문헌값(~0.146)과도 부합. 즉 기본 LSQ 결과로 Step 5 기준값 비교 흐름에 충분.

### 재측정 — 프로덕션 (Render, 2026-07-14 23:46 KST 배포 직후)

| 항목 | 기존 (2절) | 반영 후 |
|---|---|---|
| 단독 fit | 70.0초 | **25.2초(배포 직후 첫 fit·콜드) / warm 약 6.3초** |
| 단독 fit (refine_mcmc 켠 경우) | — | 11.5초 |
| 동시 6명 fit | (10명 시 중앙값 702초) | **전원 성공, 벽시계 11.9초** — 3건 즉시 + 3건 queued 이벤트(대기 약 4초) 후 실행 |
| 종료 후 health | (15명 시 502) | 200 OK |

로컬(5895) 대조: 단독 fit 0.6초(기존 MCMC 경로 15.7초), refine_mcmc 4.3초, 동시 6건 3+3 큐잉으로 전원 성공(벽시계 4.6초). 게이트 1·대기 5초로 축소한 조건에서 타임아웃 한국어 오류 이벤트와 슬롯 회수도 실측 확인. pytest 52건 통과(세마포어 큐잉·MCMC 옵트인 테스트 6건 신규).

### 운영 권고 갱신

- 6절의 "조별 순차 실행" 권고는 fit 70초 시대 기준 — **이제 15명 일제 실행도 3건씩 순차 처리되어 전원 약 30초 내 완료가 기대되고, 인스턴스 붕괴 경로(무제한 동시 MCMC)는 차단됨.** 90초 대기 한도 내에서 충분.
- 단, `refine_mcmc=true`(약 11.5초/건)를 다수가 동시에 쓰면 대기 초과(90초) 거절이 발생할 수 있음 — 현재 UI에는 켜는 경로가 없어 실습에는 영향 없음.
- 강사 노트북 로컬 백업 플랜은 여전히 권장(네트워크 장애 대비).

## 8. 재측정 ② (2026-07-16) — 방어책 후 **10·15명·동기화 버스트** 실측

### 왜 다시 했나 (7절의 구멍)

7절 재측정은 **동시 6명까지만** 실측했고, 2·4절의 10/15명 숫자는 **방어책 적용 *전*** 값이었다.
즉 "**실제 목표 인원(10~15명)이 방어책 켠 상태에서 버티는가**"는 **한 번도 측정된 적이 없고 6명 결과에서 추정**된 상태였다.
또한 **진행자가 "다 같이 누르세요" 하는 동기화 버스트**(최악 케이스)도 미측정이었다. 이 둘을 닫는 것이 목적.

### 측정 조건

| 항목 | 값 |
|---|---|
| 대상 서버 | **https://easwa-webapp.onrender.com (실제 프로덕션)** — 로컬 아님(무료 티어 성능이 병목이라 로컬은 대표성 없음) |
| 일시 | 2026-07-16 KST, 방어책(fit 게이트 3 · 측광 8 · 대기 90초 · MCMC 기본 OFF) 적용 상태 |
| 데이터 | **번들 컷아웃 `wasp_6_b_sector_0002`**(1,245프레임, 다운로드 0) = **수업시연과 동일 조건** |
| 요청 충실도 | 실제 앱과 동일 페이로드. 측광(구경 2.5/4.0/6.0, 50px) → 광도곡선 1,229점 → fit(`bjd_window` t0±0.403d, TESS, 항성 5438/4.565/−0.15, `refine_mcmc=None`=서버기본 OFF). **검증: 단독 fit 결과 Rp/R\*=0.14556 → 문헌값(~0.146)·7절(0.14534)과 일치 = 요청이 앱과 동일함이 확인됨** |
| 부하 발생 | 노트북 1대, **사용자마다 독립 httpx.AsyncClient(HTTP/1.1, 별도 커넥션)** = 기기 분리 흉내. 단일 공인 IP = **연수장 NAT와 동일 조건** |
| 서버측 검증 | fit 스트림이 대기 시 뱉는 **queued(“N번째”) 이벤트**를 캡처 |

### 결과 — **전 시나리오 통과, 크래시 0**

| 시나리오 | fit 성공 | 벽시계 전체 완료 | fit 중앙(최대) | **서버측 queued 관측** | health(after) |
|---|---|---|---|---|---|
| 기준선 1명(측광+fit) | 1/1 | 3.3초 | 1.7초 | 0/1 | 200 |
| **동시 10명**(시차 3초) | **10/10** | **29.9초** | 12.6초 (15.2) | **7/10** | 200 |
| **동시 15명**(시차 3초) | **15/15** | **48.4초** | 15.9초 (22.2) | **12/15** | 200 |
| ★ **동기화 버스트 15명**(전원 즉시 fit) | **15/15** | **45.2초** | 26.6초 (44.6) | **12/15** | 200 |

- 503/502 **0건**, 스트림 절단 0건, 모든 시나리오 후 health 200 · 60초 휴지 후에도 200.
- 개인 최대 대기 44.6초 < 대기 한도 90초 → 거절 없음.

### 단일 노트북 부하의 타당성 — **서버 자백으로 입증**

“노트북 1대의 async가 실제로 서버에 N개를 동시에 꽂는가, 아니면 클라이언트에서 직렬화되는가”는 이 방법론의 핵심 가정이다. 게이트 상한이 **3**이므로:

- **15명 → 정확히 12명 queued** (3 실행 + 12 대기)
- **10명 → 정확히 7명 queued** (3 실행 + 7 대기)

**산수가 게이트 상한과 정확히 일치.** 생성기가 직렬화 중이었다면 큐가 쌓일 수 없으므로, queued 관측은 **“서버가 실제로 N건을 동시에 받아 줄 세웠다”는 서버측 자백**이자 이 가정의 **반증 가능한 검증**이다. → 단일 노트북·단일 IP 방식은 서버 병목 측정에 유효.

### 방어 전 대비

| | 방어 전(2·4절) | **방어 후 실측(오늘)** |
|---|---|---|
| 동시 10명 | 전원 성공하나 **12분 21초** | **29.9초** |
| 동시 15명 | **인스턴스 사망, fit 0/15, health 502** | **15/15 성공, 48.4초, health 200** |

### 콜드스타트 — 부하보다 큰 실제 리스크

| 상태 | fit 소요 |
|---|---|
| **콜드(무료 티어 슬립 후 첫 요청)** | **24.1초** |
| 웜 | **1.7 ~ 2.9초** |

측정 중 첫 `/api/health` probe도 **25초 타임아웃 후 재시도에서 200**(1.4초). Render 무료는 무활동 시 빠르게 슬립.
→ **수업 5분 전 워밍업(아무 요청 1건)이 어떤 부하 대책보다 효과적.** 수업 중에는 계속 사용되므로 웜 유지됨.

### 한계 (이 측정이 대표하지 *못하는* 것)

1. **API 레벨 측정** — 브라우저 자산(JS 번들·Aladin 타일·DSS 이미지) 로딩, **연수장 Wi-Fi**, 폰/노트북 기기 차이는 **미포함**. 이 요인들은 합성 부하로 원천적으로 못 잡음 → **실기기 리허설이 유일한 검증**(권장).
2. **번들 `sector 2` 경로만 측정** — 교사가 **sector 29 등 다른 섹터/대상**을 고르면 **MAST 다운로드 경로**(ZIP 다운로드·해제·메모리)를 타며, 이는 **미측정·훨씬 무거움**. → **시연은 WASP-6 b · Sector 2로 고정 권장.**
3. 부하 중 **개인별 Rp/R\* 값 자동 대조는 미수행**(스크립트 파싱 경로 실수). 전원 `result` 이벤트 수신은 확인, 단독 fit의 값 정확성은 확인됨.

### 운영 권고 (6절·7절 갱신)

1. **동시 10~15명 일제 실행 OK** — 조별 순차/시간차 불필요. 15명 버스트도 45초 내 전원 완료. (6절의 "조별 순차" 권고는 fit 70초 시대 기준으로 **폐기**)
2. **수업 5분 전 워밍업 필수** — 콜드 24초 vs 웜 2초.
3. **시연 대상을 WASP-6 b · Sector 2로 고정** — 다른 섹터는 다운로드 경로(미측정).
4. 강사 노트북 로컬 백업 플랜은 **네트워크 장애 대비**로 여전히 권장.
5. **실기기 리허설**(폰·노트북 몇 대, 실제 연수장 Wi-Fi)로 위 한계 1을 닫을 것.

### 재현

스크립트: 세션 스크래치패드 `load_test.py`(+ `fit_body.json`, `load_results.json`) — 세션 종료 시 소멸. 재현하려면 위 "측정 조건"대로 재작성(측광 1회 → points 확보 → fit 바디 구성 → N명 동시).

---

## 9. 재현 방법·원자료 (2026-07-14 최초 측정)

- 부하 스크립트/원자료(JSON)는 세션 스크래치패드(`load_test.py`, `prod_results.json`, `local_results.json`, `prod_recovery.json`)에 생성됨 — 세션 종료 시 소멸되므로 장기 보존 필요하면 레포로 복사할 것.
- 스크립트 개요: 워밍업(`/api/health` 200 확인) → `/api/targets/wasp_6_b` + `/observations` + preview로 실제 앱과 동일한 요청 컨텍스트 구성 → 시나리오 실행(가상 사용자별 측광 스트림 → 기본 BJD 창 재현 → fit 스트림) → 시나리오별 성공/실패·소요시간·오류 유형·health 기록. 429 수신 시 Retry-After 기반 자동 재시도(최대 5회).

---

## 9. 2026-07-18 재측정 ③ — 20명 동시 + 적합 병목 규명

### 9.1 조건
1~8절과 동일 방법론(프로덕션, 번들 WASP-6 b Sector 2 50px, 앱 기본 구경 2.5/4.0/6.0,
NDJSON 스트림 끝까지 소비, 단일 IP). 적합은 앱 기본값 그대로 `refine_mcmc=False`.
합성 데이터 없음 — 실제 측광 1회로 얻은 실광도곡선(1,229점)에서 ROI 39점을 만들어
모든 가상 사용자가 같은 실제 요청을 재생.

### 9.2 결과

| | 단독 1명 | 동시 10명 | 동시 20명 |
|---|---|---|---|
| 전원 완료 | 38초 | 135.7초 | 217.3초 |
| 측광 성공 | 1/1 (1.9s) | 10/10 · p50 20.1s · max 32.4s | 18/20 · p50 55.2s · max 83.9s |
| **적합 성공** | 1/1 (34.6s) | **6/10** · p50 64.0s | **9/20** · p50 141.4s |
| 오류 | 없음 | 적합 90초 대기초과 ×4 | 적합 90초 대기초과 ×9, 측광 429 ×2 |
| 종료 후 health | 200 | 200 | 200 |

- **서버는 죽지 않는다**(4절의 15명 붕괴는 재발 없음). 게이트가 작동.
- **그러나 적합은 10명이든 20명이든 6~9명만 성공.** 인원이 늘어도 성공자는 안 늘고 실패자만 는다.
- 원인: 적합 1건 ≈ 35초(단독), 경합 시 ~65초 → 동시 3개 상한이면 90초 안에 슬롯을
  받는 건 2~3배치뿐. **`STREAM_QUEUE_WAIT_SECONDS=90`이 병목의 핵심.**
- 측광 429는 연수장 NAT(전원 동일 공인 IP) + `TRANSIT_PHOTOMETRY_LIMIT=6/분` 조합에서 발생.
- 콜드스타트 재확인: 첫 API 호출 23.4초 vs 웜 1.7~2.6초.

### 9.3 적합 단계별 프로파일 (스트림 이벤트 타임스탬프)
init/phase_fold/preprocess 0.4초 · **least_squares 13~29초** · finalizing 0.2초.
→ 시간의 거의 전부가 least_squares.

원인: `_evaluate_batman`이 잔차마다 `batman.TransitModel`을 재생성(생성자는 적분 스텝을
반복 탐색). 시작점 5개 × `max_nfev=300` ≈ 1,500회 재생성.
**수정(커밋 18dfbdc)**: 표본 시각 배열을 옮기는 대신 에폭(`params.t0`)을 옮겨 배열을 고정하고
`(times, exp_time)` 키로 모델 캐시 → 재사용. 등가성 검증 **최대 절대오차 0.0**(동일), 속도 약 2배.
남은 절반은 batman 계산 자체이며, 지배 요인은 Render 공유 CPU(로컬 대비 약 30배 느림).

### 9.4 다중 시작점 최적화 — 실데이터 검증 결과 **기각**
"시작점 5개 중 1개만 쓰면 5배 빨라진다"를 실광도곡선 기반 8개 시나리오(ROI 폭·주변광감광·
baseline 차수 변화)로 검증:

- **8/8에서 승자가 start 0이 아니었다.**
- 7/8에서 start 0만 쓰면 Rp/Rs가 1% 이상 어긋남(33%, 20.8%, 25.6%, …).
- **2건은 start 0이 하한 0.00100으로 붕괴**("행성 없음" 해) — 다중 시작이 건져냄.
- 조건부 조기종료도 불가: 붕괴는 탐지되지만 20~33% 어긋나는 케이스는 start 0 결과가
  겉보기에 정상(b≈0.24)이라 **탐지 불가능한 조용한 오답**이 된다.

→ **다중 시작점은 유지한다. 이 최적화는 다시 시도하지 말 것.**
(단서: 검증 시 t0를 가장 어두운 단일 점으로 잡아 절대값이 문헌값 0.155와 다름 —
초기추정이 나쁜 스트레스 조건. 학생은 창을 눈대중으로 잡으므로 현실적이며 결론은 더 강해짐.)

### 9.5 남은 조치 (env, 코드 변경 아님)
| 설정 | 현재 | 권고 | 근거 |
|---|---|---|---|
| `EASWA_STREAM_QUEUE_WAIT_SECONDS` | 90 | **300** | 20명 전원 처리에 ~180초 필요 |
| `EASWA_TRANSIT_PHOTOMETRY_LIMIT` | 6/분 | **40/분** | 교실 전체가 공인 IP 하나를 공유 |

운영 병행: 조별 시차 실행(4~5명) + 수업 직전 워밍업.

### 9.6 env 조치 후 재측정 — **20명 전원 성공** (2026-07-18)

`EASWA_STREAM_QUEUE_WAIT_SECONDS=300`, `EASWA_TRANSIT_PHOTOMETRY_LIMIT=40` 적용 후
9.1과 동일한 방법론·동일 스크립트로 재측정.

| | 조치 전 | **조치 후** |
|---|---|---|
| 10명 적합 | 6/10 | **10/10** |
| **20명 적합** | 9/20 | **20/20** |
| 20명 측광 | 15/20 | **20/20** |
| 429 | 5건 | **0건** |
| 큐 타임아웃 | 6건 | **0건** |
| 종료 후 health | 200 | 200 |

20명 동시 상세: 전원 완료 **356.1초**, 적합 p50 178.7초 / p90 257.3초 / **최대 263.6초**,
측광 p50 58.9초 / 최대 94.1초. 단독 적합은 25.5초(9.3절 batman 최적화 반영 후).

**해석**: 처리량이 는 게 아니라 **"실패"가 "대기"로 바뀐 것**이다. 동시 실행 상한(3)은
그대로이므로 총 소요는 오히려 늘었다(217초→356초). 이전의 217초는 11명이 90초에
튕겨나간 시간이었다.

**반영 여부 판별법**: 큐 타임아웃 에러 문구가 설정값을 그대로 찍는다
(`"... {STREAM_QUEUE_WAIT_SECONDS}초 동안 차례가 오지 않았습니다"`). 문구가 "90초"면 미반영.
config.py는 프로세스 시작 시 1회만 env를 읽으므로 **저장 후 재시작이 필요**하다.

### 9.7 수업 운영 권고 (실측 기반)

| 방식 | 개인 최대 대기 | 비고 |
|---|---|---|
| 20명 전체 동시 | **약 4.4분** | 전원 성공하나 체감 대기 큼 |
| **4~5명씩 조별 · 1분 간격** | **약 1분 이내** | 권장 |

- 적합 1건 약 25~29초 × 동시 3개 상한 → 조를 5명 이하로 끊으면 조당 약 1분에 소화된다.
- **수업 직전 워밍업 필수**: 콜드 첫 호출 23.4초 vs 웜 0.5~2.6초. 부하보다 콜드스타트가 크다.
- 적합 진행 카드의 **중지** 버튼(커밋 abae309)은 **그 학습자를 기다림에서 풀어줄 뿐,
  서버 슬롯을 즉시 돌려주지는 않는다** (9.8절 실측 — 앞선 커밋 메시지의 "즉시 반환"
  주장은 틀렸다). 버려진 적합은 계산을 끝까지 마친 뒤에야 슬롯을 놓는다.

### 9.8 엣지케이스 실측 (2026-07-18) — 중지·중복요청·잘못된 입력

#### 잘못된 입력: 전부 안전, 500 없음
| 입력 | 응답 |
|---|---|
| 음수/0 주기 | `"양수인 공전 주기가 필요합니다"` 에러 이벤트 |
| 점 5개 / 0개 | `"최소 20개 광도곡선 점이 필요합니다"` |
| magnitude=null | 422 검증 오류 |
| BJD 구간 뒤집힘 | 200 정상(min/max로 흡수) |

학습자가 이상한 값을 넣어도 서버 오류(500) 없이 이유를 설명한다.

#### 중지/연결 끊김: **슬롯은 즉시 반환되지 않는다**
적합 3건으로 게이트를 채우고 t=3초에 전부 연결을 끊은 뒤 확인:

| 확인 시점 | 결과 |
|---|---|
| t≈12초 | 여전히 큐 대기 (슬롯 점유 중) |
| t≈25초 | 여전히 큐 대기 |
| 완전 드레인 후 단독 적합 | 11.6초, 대기 없음, 정상 → **영구 누수는 아님** |

원인: 라우트의 `finally: gate.release()`는 Starlette이 클라이언트 끊김을 감지해야
돌는데, 서버는 스레드풀에서 다음 이벤트를 기다리는 동안 전송을 시도하지 않아
끊김을 알아채지 못한다. 버려진 적합은 계산을 끝까지 마친 뒤에야 슬롯을 놓는다.

**따라서 중지 버튼의 실제 효과는 "그 학습자가 기다림에서 벗어나는 것"까지이고,
다른 학습자의 차례를 앞당기지는 않는다.** (커밋 a2f8140·abae309 메시지의 "즉시 반환"
서술은 이 실측으로 정정됨.)

고치려면 게이트 스트림 루프에서 `request.is_disconnected()`를 이벤트 사이에 확인해
조기 종료해야 한다. 미적용 — 시연 직전 동시성 코드 변경은 위험 대비 이득이 낮다고 판단.

#### 측정 함정 메모 (반복 방지)
- 첫 시도는 끊은 뒤 **1초 만에** 확인해 "슬롯 누수"로 오판했다. 적합은 진행 이벤트를
  2~3초 간격으로 내보내므로 그 전에는 서버가 끊김을 알 수 없다. **최소 한 이벤트 주기
  이상 기다린 뒤 확인할 것.**
- 동일 ROI로 적합을 반복하면 batman 모델 캐시(9.3절)에 적중해 11초대로 빨라진다.
  학습자마다 ROI가 달라 캐시가 갈리므로 **이 숫자를 실제 수업 값으로 쓰면 안 된다.**

### 9.9 익명 시트 자동저장 부하 (2026-07-18) — 20명 동시 저장

지금까지의 측정은 전부 Render(측광·적합)였다. **자동저장은 다른 인프라**(Google
Apps Script 시트)라 한 번도 측정되지 않았다. 수신부는 `LockService.getScriptLock()`
+ `waitLock(20000)`으로 **모든 쓰기를 전역 직렬화**한다.

| 동시 저장 | 성공 | 중앙값 | 최대 | 드레인 |
|---|---|---|---|---|
| 1 | 1/1 | 3.15s | 3.15s | 3.1s |
| 5 | 5/5 | 5.62s | 6.29s | 6.3s |
| 10 | 10/10 | 6.74s | 11.32s | 11.3s |
| **20** | **20/20** | 10.27s | **18.99s** | **19.0s** |

**전부 성공했지만 20명은 경계선이다**: 마지막 쓰기가 18.99초, 락 대기 상한이 20초 —
**여유 1초(5%)**. 10명은 11.3초로 여유 43%라 안전하다.

**이 수치는 오히려 낙관적이다.** 시트 오염을 피하려 모든 요청을 같은 `anon_id`로
보냈고, 스크립트는 `(anon_id, target_id)` upsert이므로 **기존 행 1개를 갱신하는 빠른
경로**를 탔다. 실제 수업은 서로 다른 행 20개를 append하고, 시트가 커질수록
`findRow_`의 스캔도 느려진다 → 실전 여유는 이보다 작다.
(측정 기법 메모: 같은 id로 보내면 아무리 두들겨도 행이 1개만 생겨 오염 없이 락
경합을 잴 수 있다. 재측정 시 이 방법을 쓸 것.)

#### 조치 (커밋 0b2feb0 — 커밋 메시지가 printf `%)` 때문에 잘려 여기 남긴다)
- **디바운스에 0~2.5초 지터**: 고정 4초는 같은 단계를 함께 끝낸 학급이 **정확히 같은
  순간** 발사하게 만들어 버스트를 키운다. 시작 시각을 흩는 것이 락 경합을 줄이는
  근본 대책이다.
- **실패 시 6초 뒤 1회 재시도**: 타임아웃되면 sink가 `busy, retry`를 돌려주는데
  프론트는 재시도하지 않고 학습자의 다음 편집까지 방치했다. 화면엔 "이 브라우저에만
  저장됨"만 뜨고 **연구 데이터는 조용히 유실**된다. 1회 재시도로 버스트는 덮되
  재시도 폭풍은 되지 않게 했다. 이펙트 cleanup이 두 타이머를 모두 취소하므로 대상
  변경·이탈 시 유령 전송은 없다.

#### 미측정으로 남은 것
**로그인 draft 자동저장**(Render SQLite)은 인증이 필요해 재지 못했다. 코드상 우려:
디바운스가 **600ms로 익명(4초)의 6배**이고 envelope에 photometry 응답 전체(광도곡선
1,229점)가 들어가 요청 하나가 수백 KB급이다. 시연은 대부분 비로그인이라 우선순위는
낮지만, 로그인 학습자가 많은 수업 전에는 별도 확인이 필요하다.
