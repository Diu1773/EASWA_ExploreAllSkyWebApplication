# -*- coding: utf-8 -*-
"""v14 — 개발 방식 검증을 ①② 로 옮기고 ③ 은 결과물 검증으로 (2026-09-11).

사장님 지시 —
  *「바이브코딩이나 ai에이전트쪽엔 claude codex 로고 넣자」*
  *「그래서 값을 따로 검증했음 이거말고, 결과물 검증하고 개발방식검증은 바이브코딩이나
    에이전트 쪽에서 넣고」*
  *「ai에이전트 도식이나 그림넣으면 좋을듯」*

  ① 바이브 코딩   — 무엇인가 · 사례 넷 · «우리가 쓴 범위»
  ② AI 에이전트   — 도식 · 도구 로고 · 채택 규모 · Koch · «우리가 정한 경계»
  ③ 결과물 검증   — 산출값과 화면 문장

로고는 `make_logos.py`(simple-icons CC0), 에이전트 도식은 `fig_agent.py` 가 만든다.
**남의 그림을 옮기지 않는다** — 인용 문헌의 서술을 근거로 직접 그렸다.
"""
import io
import os

HERE = os.path.dirname(os.path.abspath(__file__))
P = os.path.join(HERE, "build.py")
s = io.open(P, encoding="utf-8").read()

# ══ ① 끝에 우리가 쓴 범위 한 줄 ═══════════════════════════════════════
old = '''    ("→ 도메인을 아는 사람이 «자기 문제를 푸는 도구»를 직접 만드는 흐름. 이 연구도 그 자리에 있음",
     ACC, True),
], size=15, gap=9)'''
new = '''    ("→ 도메인을 아는 사람이 «자기 문제를 푸는 도구»를 직접 만드는 흐름. 이 연구도 그 자리에 있음",
     ACC, True),
    ("이 연구에서 쓴 범위 — 수업 맥락을 아는 연구자가 프론트엔드·백엔드 코드 초안, "
     "오류 수정, 반복 구현에 사용함", BODY, False),
], size=15, gap=8)'''
assert s.count(old) == 1
s = s.replace(old, new)

# ══ ② AI 에이전트 — 도식과 로고를 넣고 경계를 적는다 ══════════════════
i = s.index('y = title(sl, "개발 방식 ② AI 에이전트",')
i = s.rindex("# ═════", 0, i)
j = s.index("# ═════", i + 10)
agent = '''# ═════ 개발 방식 ② AI 에이전트 ═══════════════════════════════════════
sl = S()
y = title(sl, "개발 방식 ② AI 에이전트",
          "목표를 주면 저장소를 읽고, 고치고, 명령을 돌려 확인하기를 스스로 되풀이함")
pic(sl, "fig_agent.png", M, y + 0.02, W - 2 * M, 2.42, root=HERE, top=True)
pic(sl, "fig_tools.png", M, y + 2.50, W - 2 * M, 1.02, root=HERE, top=True)
bullets(sl, M, y + 3.62, W - 2 * M, [
    ("공개 저장소 1억 8천만 개를 훑은 조사에서 에이전트 커밋이 월 32만 건을 넘음. "
     "한 도구만도 17,295개 프로젝트에서 886,122 커밋 (Khosravani and Mockus, 2026)",
     BODY, False),
    ("「중심 문제는 더 이상 프롬프트 공학이 아니라 공학적 과정 통제다」 (Koch, 2026)",
     WARN, True),
    ("→ 이 연구가 정한 경계 — 탐구 구조와 단계 설계, 학습자에게 개방할 분석 조건, "
     "천문 모델과 가정은 «연구자가» 정함", ACC, True),
], size=14, gap=7)
cite(sl, "arXiv:2606.24429 · 2605.20456. 2026-09-11 초록 확인. "
         "로고는 simple-icons(CC0)이며 상표는 각 소유자의 것임.")

'''
s = s[:i] + agent + s[j:]

# ══ ③ 결과물 검증 ════════════════════════════════════════════════════
i = s.index('y = title(sl, "개발 방식 ③ 그래서 값을 따로 검증했음",')
i = s.rindex("# ═════", 0, i)
j = s.index("# ═════", i + 10)
verify = '''# ═════ 개발 방식 ③ 결과물 검증 ═══════════════════════════════════════
sl = S()
y = title(sl, "개발 방식 ③ 결과물 검증",
          "화면이 도는 것과 값이 맞는 것은 별개임")
bullets(sl, M, y + 0.10, W - 2 * M, [
    ("생성된 코드는 오류 없이 실행되면서도 산출값이 틀릴 수 있음 (Uddin, 2026)",
     WARN, True),
    ("", BODY, False),
    ("산출값 — 분석 결과를 문헌값과 대조함. 같은 자료·같은 설정을 반복 실행하면 "
     "WASP-6 b 반지름비가 0.14534 로 재현됨", BODY, False),
    ("처리 조건을 바꾸었을 때의 민감도 점검은 부록 1에 두었음", BODY, False),
    ("화면 문장 — 안내 문장도 학습자의 판단 근거이므로 사용자 검토로 함께 점검함",
     BODY, False),
    ("", BODY, False),
    ("→ 산출값이 학습 활동의 근거가 되는 도구에서는 «화면의 동작»과 «값의 타당성»을 "
     "각각 확인해야 함", ACC, True),
    ("개발 방식 간 비교는 하지 않았음. 시간·비용을 기록하지 않아 빨랐다고 말할 수 없음",
     GREY, False),
], size=16, gap=10)

'''
s = s[:i] + verify + s[j:]

io.open(P, "w", encoding="utf-8", newline="\n").write(s)
print("① 에 쓴 범위 · ② 에 도식·로고·경계 · ③ 을 결과물 검증으로")
